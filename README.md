# Magnet-to-Torrent
A lightweight PyQt5 desktop app for Linux (built for Debian/KDE) that converts magnet links into real .torrent files and uploads them straight to a directory on a remote server over SSH/SCP.  No browser extensions, no third-party web services — everything runs locally and talks directly to your own server.
